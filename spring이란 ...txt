정적 웹 : 80
동적 웹 : 8080
보안서버 : 443

의존성 주입

"스프링 컨테이너에서 객체 Bean을 먼저 생성해두고 생성한 객체를 지정한 객체에 주입하는 방식을 의존성 주입 이라고 합니다"
DI : 새로운 생성자에 주입해주는것

프레임워크 자바에서 쓰는 프레임워크 내가 움직이기위한 규칙?

컨텍스트 = 환경 
빈들은 환경에 정의된다

Configuration?

XML을 자바버전으로 바꾼것

ioc 
빈
인스턴스를 스프링에서 대신 생성해주겠다

di 의존성 주입

빈들로 생성되어있는 녀석들으 주입해주는것이 di의 역할


@spring boot 메인메서드 밑에 붙어있어서 다른아이들도 생략되어있음 
어노테이션(@) 

dao


람다 표현식

@qualifier 직접지정

@오토와이어드 자동으로 빈을 지정해준다
@프라이머리 키를 만들어서 지정
@컴포넌트 빈으로 등록해줄떄 쓰는것
